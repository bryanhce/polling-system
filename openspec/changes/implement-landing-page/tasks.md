## 1. Landing experience

- [x] 1.1 Replace the starter `App` UI with semantic landing-page landmarks,
  product messaging, create CTA, and join-poll form.
- [x] 1.2 Implement client-side poll ID/link normalization, inline validation,
  and navigation to the appropriate poll destination.

## 2. Visual system and responsiveness

- [x] 2.1 Replace starter global styles with the documented product tokens,
  typography, focus treatment, and reduced-motion support.
- [x] 2.2 Add the responsive landing-page layout, bubble-card styling, and
  non-essential CSS decoration.

## 3. Verification

- [x] 3.1 Run the frontend build and lint checks and address any failures.

## 4. Tailwind CSS adoption

- [x] 4.1 Install `tailwindcss` and `@tailwindcss/vite`, register the Vite
  plugin, and import Tailwind from the global stylesheet.
- [x] 4.2 Define the product semantic tokens as Tailwind v4 theme variables
  and migrate the landing page layout, responsive rules, and interaction
  states from authored CSS to utilities.
- [x] 4.3 Retain only necessary CSS for decorative pseudo-elements, then run
  the frontend build and lint checks.
